import React from 'react';

const BtnCatch = (props) => {
    return (
        <button href="#" className="btn btn-primary btn-block btn-lg" onClick={props.onClick}>Catch me!</button>
    )
}

export default BtnCatch;